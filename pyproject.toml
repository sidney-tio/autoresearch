[project]
name = "prompt-evasion"
version = "0.1.0"
description = "Autoresearch-style prompt optimization: evade an AI-text detector"
requires-python = ">=3.10"
dependencies = [
    "vllm>=0.6.0",
    "transformers>=4.44.0",
    "openai>=1.40.0",
    "langdetect>=1.0.9",
]
