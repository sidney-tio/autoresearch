# Python
__pycache__/
*.py[oc]
build/
dist/
*.egg-info

# Virtual environments
.venv/

# Run artifacts (regenerated every iteration)
run.log
last_run.jsonl
best_run.jsonl

# Experiment log — kept untracked on purpose (see CLAUDE.md)
results.tsv
